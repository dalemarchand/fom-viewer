# fom-viewer
A single page html file for visualizing IEEE 1516 compliant Federation Object Model (FOM) files

## Testing

```bash
cd test
npm install
npm test
```

For visible browser:
```bash
npm run test:visible
```

See `test/README.md` for more options.
